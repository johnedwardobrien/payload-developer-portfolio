import React from 'react'

type Props = {
  disableInnerContainer?: boolean
  [key: string]: unknown
}

export const VideoCards: React.FC<Props> = (props) => {
  console.log('VideoCards data:', props)

  return <></>
}
